# CCUBaseball pulls CCU baseball batting statistical data from a SQL database, processes the information a few different ways, and returns the data to the user through the search bar with an API layer. 
